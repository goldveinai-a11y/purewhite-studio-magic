Я проверил текущий код: AI Gateway логов за неделю нет, значит Lovable AI/QC-судья реально не вызывается. Но в коде остались медленные/старые хвосты, которые объясняют 12 секунд на 3 фото:

1. В `process-image.functions.ts` QC-судьи уже нет, но остались fallback-ветки `birefnet/bria` и старые комментарии про QC.
2. В `studio-workspace.tsx` каждый fal-запрос делается через `removeWithRetry`: при ошибке rembg он повторяет rembg, а потом может уйти в `birefnet` fallback — это может резко увеличить время.
3. Сейчас `MAX_CONCURRENT = 12`, хотя требование было `8 concurrent`, и UI тоже показывает 12. Для 3 фото это не всегда быстрее: можно получить перегруз/очередь на внешнем API.
4. В `canvas-processing.ts` остались `toJudgeThumb` и комментарии про AI QC; функция не используется, но это мусор и источник путаницы.
5. В лендинге остался текст “AI quality inspection completes…” — визуально обещает QC, хотя фактически его нет.

План правок:

- Полностью зачистить QC-наследие:
  - удалить `toJudgeThumb` из `canvas-processing.ts`;
  - убрать комментарии про AI QC в `process-image.functions.ts` и `canvas-processing.ts`;
  - обновить маркетинговый текст, чтобы больше не упоминать “AI quality inspection”.

- Сделать backend строго `rembg-only`:
  - `removeBackground` будет принимать только imageUrl/preUpscale;
  - убрать публичную возможность выбрать `birefnet/bria`;
  - убрать fallback на `birefnet`, чтобы один файл не мог внезапно уйти в 6-10 секунд.

- Ускорить и стабилизировать batch:
  - убрать double retry по умолчанию или заменить на один короткий retry только для transient failure без перехода на другую модель;
  - вернуть лимит параллельности к 8;
  - обновить текст dropzone на “8 concurrent”.

- Сохранить качество текущего результата:
  - не включать агрессивный debris;
  - оставить rembg + текущий canvas-постпроцессинг;
  - не возвращать ручную HD-эскалацию и не возвращать QC-судью.

Ожидаемый результат: минус скрытые медленные ветки, понятный rembg-only pipeline, отсутствие QC-затрат, и 3 фото должны обрабатываться ближе к времени самого rembg + canvas, без внезапного ухода в Birefnet.