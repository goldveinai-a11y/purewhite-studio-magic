# План: производительность и layout studio

## 1. preUpscale — оставить, поднять порог 900 → 1200px
Файл: `src/lib/process-image.functions.ts` (или там, где живёт условие preUpscale).
- Меняем условие срабатывания: `if (maxSide < 1200)` вместо `< 900`.
- Логика Recraft-апскейла и Amazon 1000×1000 через canvas остаётся без изменений.
- Комментарий в коде: «Amazon требует ≥1000px, порог 1200 = 20% запас».

## 2. Silent retry — убрать
Файл: `src/components/studio-workspace.tsx` (и, если нужно, `process-image.functions.ts`).
- Убрать автоматический повтор при ошибке/таймауте `rembg`.
- При сбое: job переходит в статус `error`, показываем честное сообщение + кнопку **Retry** на карточке фото в очереди.
- Пользователь сам решает, повторять или нет. Время обработки становится предсказуемым.

## 3. «8 concurrent» — убрать из UI, в коде оставить
Файл: `src/components/studio-workspace.tsx` (dropzone-текст).
- В UI dropzone: «Up to 50 photos at once. JPG, PNG, WebP.» (без упоминания concurrent).
- Константа `MAX_CONCURRENT = 8` в коде остаётся.

## 4. Layout workspace — перестроить
Файл: `src/components/studio-workspace.tsx`.

Новая структура (сверху вниз, full-width):

```text
┌─────────────────────────────────────────────┐
│  Dropzone (компактный, свернутый после      │
│  первой загрузки — «+ Add more photos»)     │
├─────────────────────────────────────────────┤
│                                             │
│      КРУПНЫЙ PREVIEW (Before/After)         │
│         выбранного фото, full-width         │
│                                             │
├─────────────────────────────────────────────┤
│  Settings row (Amazon preset, Soft shadow)  │
├─────────────────────────────────────────────┤
│  Queue: горизонтальная лента миниатюр       │
│  [thumb][thumb][thumb]…  ← клик = выбрать   │
│  Статус — маленькая точка/иконка в углу     │
│  миниатюры (без больших перекрывающих       │
│  бейджей). Ошибка → красная точка + Retry   │
│  по клику.                                  │
├─────────────────────────────────────────────┤
│  Actions: Download all (ZIP), Clear         │
└─────────────────────────────────────────────┘
```

Правки:
- Заменить текущий двухколоночный grid на вертикальный stack.
- Preview — крупный (min-height ~500px, сохранять aspect).
- Очередь — `flex overflow-x-auto` с миниатюрами ~80×80, статус точкой в углу (`bg-green-500` done / `bg-amber-500` processing / `bg-red-500` error).
- Убрать текстовые бейджи поверх миниатюр.
- Selected thumb — рамка `ring-2 ring-primary`.

## Что НЕ трогаем
- Маркетинговые формулировки про AI inspection на лендинге — остаются.
- Pipeline rembg (primary) → всё как сейчас, никаких новых моделей.
- Canvas post-processing (тени, feathering, Amazon 1000×1000) — без изменений.
- Кредитная логика и paywall — без изменений.

## Проверка после реализации
- Загрузить 3 больших фото → preUpscale НЕ срабатывает, время ≈ 3–5 сек на батч.
- Загрузить фото 800px → preUpscale срабатывает (порог 1200), выход резкий.
- Симулировать ошибку rembg → карточка показывает Retry, никаких скрытых повторов.
- В dropzone нет текста про «concurrent».
- Preview крупный, миниатюры в ленте, бейджи не перекрывают фото.
